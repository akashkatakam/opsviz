apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsviz-chart.fullname" . }}-test-connection"
  labels:
    {{- include "opsviz-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsviz-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
